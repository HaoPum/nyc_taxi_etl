FROM apache/superset:latest

# Switch to root user to install packages
USER root

# Install the required Python packages for PostgreSQL and Trino connections
RUN /app/.venv/bin/pip install psycopg2-binary sqlalchemy-trino

# Switch back to the default superset user
USER superset
